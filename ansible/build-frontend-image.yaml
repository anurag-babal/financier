---
- hosts: localhost
  tasks:
    - name: Build frontend image
      docker_image:
        name: "{{ microservice_name }}:latest"
        state: present
        build:
          path: frontend
          dockerfile: Dockerfile