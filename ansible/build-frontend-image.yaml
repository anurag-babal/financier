---
- hosts: localhost
  tasks:
    - name: Build frontend image
      docker_image:
        name: "{{ microservice_name }}:latest"
        build:
          path: ../frontend
        source: build
        state: present
