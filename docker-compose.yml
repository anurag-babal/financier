# Docker Compose file for running the Financier application in a local development environment.
# Version '3.8' is a modern and stable version of the Docker Compose file format.
# version: "3.8"

services:
  #----------------------------------------------------#
  #  INFRASTRUCTURE SERVICES
  #----------------------------------------------------#
  service-registry:
    build:
      context: ./infrastructure/service-registry
    container_name: service-registry
    ports:
      - "${FINANCIER_REGISTRY_PORT}:8761"
    networks:
      - financier-net

  api-gateway:
    build:
      context: ./infrastructure/api-gateway
    container_name: api-gateway
    ports:
      - "${FINANCIER_GATEWAY_PORT}:8080"
    depends_on:
      - service-registry
    environment:
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_HOST}:8761/eureka
      - JWT_SECRET=${JWT_SECRET}
      - ALLOWED_ORIGIN=${ALLOWED_ORIGIN}
    networks:
      - financier-net

  message-broker:
    image: rabbitmq:3.9-management
    container_name: message-broker
    ports:
      - "5672:5672"
      - "15672:15672"
    networks:
      - financier-net

  #----------------------------------------------------#
  #  DATABASES
  #----------------------------------------------------#
  user-db:
    image: postgres:14
    container_name: user-db
    ports:
      - "${POSTGRES_PORT}:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - user-db-data:/var/lib/postgresql/data
    networks:
      - financier-net

  expense-db:
    image: mongo:5.0
    container_name: expense-db
    ports:
      - "${MONGO_PORT}:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_USER}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_PASSWORD}
    volumes:
      - expense-db-data:/data/db
    networks:
      - financier-net

  #----------------------------------------------------#
  #  BACKEND SERVICES
  #----------------------------------------------------#
  user-service:
    build:
      context: ./services/user-service
    container_name: user-service
    ports:
      - "${FINANCIER_USER_SERVICE_PORT}:8081"
    depends_on:
      - user-db
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
      - SPRING_DATASOURCE_USERNAME=${POSTGRES_USER}
      - SPRING_DATASOURCE_PASSWORD=${POSTGRES_PASSWORD}
      - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${EUREKA_HOST}:8761/eureka
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - financier-net

  expense-service:
    build:
      context: ./services/expense-service
    container_name: expense-service
    ports:
      - "${FINANCIER_EXPENSE_SERVICE_PORT}:8082"
    depends_on:
      - expense-db
      - service-registry
      - message-broker
    environment:
      - PORT=${FINANCIER_EXPENSE_SERVICE_PORT}
      - MONGO_URI=mongodb://${MONGO_USER}:${MONGO_PASSWORD}@${MONGO_HOST}:${MONGO_PORT}
      - RABBITMQ_URL=amqp://${RABBITMQ_USER}:${RABBITMQ_PASSWORD}@${RABBITMQ_HOST}:5672/
      - EUREKA_URL=http://${EUREKA_HOST}:8761/eureka
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - financier-net

  # --- Add other services (budget, reporting, notification) here in a similar fashion ---

#----------------------------------------------------#
  #  FRONTEND SERVICE
  #----------------------------------------------------#
  # web-app:
  #   build:
  #     context: ./apps/web
  #   container_name: web-app
  #   ports:
  #     - "3000:80"
  #   depends_on:
  #     - api-gateway
  #   networks:
  #     - financier-net

#----------------------------------------------------#
#  VOLUMES & NETWORKS
#----------------------------------------------------#
volumes:
  user-db-data:
  expense-db-data:

networks:
  financier-net:
    driver: bridge
